# Scripts that run on the Linux database hosts. CRLF from a Windows checkout
# breaks them with "bad interpreter" or syntax errors.
*.sh text eol=lf
patroni.yml text eol=lf
docker-compose.yml text eol=lf
