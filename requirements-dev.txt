# CI quality gate only (lint, typecheck, tests); not installed in the image.
-r requirements.txt

pytest==9.1.1
# pytest-asyncio 1.0 caps pytest below 9.
pytest-asyncio==1.4.0
ruff==0.14.4
mypy==1.18.2
# Validates the /watch NDJSON builders against their JSON Schema.
jsonschema==4.25.1
